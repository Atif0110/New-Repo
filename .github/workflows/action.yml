name: Custom Action

on:
  push:
    branches:
      - main # The action triggers when changes are pushed to the main branch
  workflow_dispatch:  # This allows you to manually trigger the action

jobs:
  test:
    runs-on: ubuntu-latest  # Runs the job on the latest Ubuntu image

    steps:
      - name: 22f3001664@ds.study.iitm.ac.in  # Step name contains your email
        run: echo "Hello, world!"  # Runs a simple shell command
